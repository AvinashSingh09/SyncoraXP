{
  "name": "voice-translation-platform",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "pnpm --parallel --filter @voice/api --filter @voice/web dev",
    "dev:lan": "pnpm --parallel --filter @voice/api --filter @voice/web dev:lan",
    "build": "pnpm -r --if-present build",
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test"
  },
  "dependencies": {
    "pnpm": "^10.34.5"
  }
}
